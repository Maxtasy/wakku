package com.maxtasy.wakku.ui.theme

import androidx.compose.material3.Typography

// Default Material 3 type scale for now; revisit once the app has a real
// typeface (see Milestone 7 — polish & compatibility).
val Typography = Typography()
